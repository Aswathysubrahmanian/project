var a=4 //can use var for declaring a variable as globally
let b=5 // can use var for declaring a variable as only locally
const c=true //constant variable
var x="luminar"
console.log(typeof(c))
// types number,string,boolean,undefined(unused data),null